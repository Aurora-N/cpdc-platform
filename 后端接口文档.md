# 社区互动模块 - 后端接口文档

## 基础信息
- **基础URL**: `/api`
- **分页参数**: `page` (默认1), `size` (默认20)
- **统一返回格式**: 
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

## 接口列表

### 1. 获取帖子列表（分页）

**接口**: `GET /api/posts`

**请求参数**:
- `page` (Integer, 可选): 页码，默认1
- `size` (Integer, 可选): 每页数量，默认20
- `keyword` (String, 可选): 搜索关键词（标题或内容）

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "帖子标题",
        "content": "帖子内容",
        "authorId": 1,
        "authorName": "用户1",  // 需要关联用户表获取
        "authorAvatar": "头像URL",  // 可选
        "images": ["url1", "url2"],  // JSON数组或逗号分隔的字符串
        "watchTimes": 10,
        "likedTimes": 5,
        "replyCount": 3,
        "createdTime": "2025-10-21 10:00:00",
        "updateTime": "2025-10-21 10:00:00",
        "isLiked": false  // 当前用户是否已点赞（需要根据当前登录用户判断）
      }
    ],
    "total": 100,
    "pages": 20,
    "current": 1,
    "size": 5
  
}
```

**注意**:
- `images` 字段：如果数据库中存储的是 JSON 字符串，需要解析为数组；如果是逗号分隔的字符串，需要转换为数组
- `authorName` 需要 JOIN 用户表获取用户名
- `isLiked` 需要根据当前登录用户判断是否已点赞（需要用户点赞关联表）

---

### 2. 获取帖子详情

**接口**: `GET /api/posts/{id}`

**路径参数**:
- `id` (Long): 帖子ID

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "帖子标题",
    "content": "帖子内容",
    "authorId": 1,
    "authorName": "用户1",
    "authorAvatar": "头像URL",
    "images": ["url1", "url2"],
    "watchTimes": 10,
    "likedTimes": 5,
    "replyCount": 3,
    "createdTime": "2025-10-21 10:00:00",
    "updateTime": "2025-10-21 10:00:00",
    "isLiked": false
  }
}
```

**注意**:
- 访问帖子详情时，需要增加 `watchTimes`（浏览数）

---

### 3. 创建帖子

**接口**: `POST /api/posts`

**请求头**:
- `token`: 用户登录token（必需）

**请求体**:
```json
{
  "title": "帖子标题",  // 最多20字
  "content": "帖子内容",  // 最多100字
  "images": ["url1", "url2"]  // 可选，最多6张图片
}
```

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": 1  // 新创建的帖子ID
}
```

**注意**:
- 需要验证用户登录状态
- 需要验证标题和内容长度限制
- `images` 数组需要转换为字符串存储到数据库（建议使用 JSON 格式）

---

### ==4. 点赞/取消点赞帖子 先直接在前端+1 不需要返回点赞数 点赞异常弹窗==

**接口**: `POST /api/posts/{id}/like`

**路径参数**:
- `id` (Long): 帖子ID

**请求头**:
- `token`: 用户登录token（必需）

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "liked": true,  // 当前是否已点赞
    "likedTimes": 6  // 当前点赞总数
  }
}
```

**注意**:
- 需要维护用户点赞关联表（如 `user_post_like`），记录用户是否已点赞
- 如果用户已点赞，则取消点赞；如果未点赞，则点赞
- 需要更新 `interaction_post` 表的 `liked_times` 字段

---

### 5. 获取帖子评论列表（分页）

**接口**: `GET /api/posts/{postId}/replies`

**路径参数**:
- `postId` (Long): 帖子ID

**请求参数**:
- `page` (Integer, 可选): 页码，默认1
- `size` (Integer, 可选): 每页数量，默认20

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "postId": 1,
        "answerId": 0,  // 0表示回复帖子，否则是回复的评论ID
        "userId": 2,
        "userName": "用户2",  // 需要关联用户表
        "userAvatar": "头像URL",  // 可选
        "content": "评论内容",
        "targetUserId": null,  // 回复的目标用户ID
        "targetReplyId": null,  // 回复的评论ID
        "targetUserName": null,  // 目标用户名称
        "images": ["url1"],  // 可选
        "createdTime": "2025-10-21 10:00:00",
        "updateTime": "2025-10-21 10:00:00",
        "children": []  // 子评论列表（多级回复）
      }
    ],
    "total": 50,
    "pages": 5,
    "current": 1,
    "size": 10
  }
}
```

**注意**:
- 需要构建评论的树形结构（根据 `answerId` 字段）
- `answerId = 0` 表示顶级评论（直接回复帖子）
- `answerId != 0` 表示回复某个评论
- 需要关联用户表获取 `userName`
- 如果 `targetUserId` 不为空，需要获取 `targetUserName`

---

### 6. 创建评论 ==改接口链接==

**接口**: `POST /api/posts/{postId}/replies`

**路径参数**:

- `postId` (Long): 帖子ID

**请求头**:
- `token`: 用户登录token（必需）

**请求体**:
```json
{
  "S": "评论内容",  // 最多255字
  "answerId": 0,  // 可选，0或不传表示回复帖子，否则是回复的评论ID
  "targetUserId": 1,  // 可选，回复的目标用户ID
  "targetReplyId": 1,  // 可选，回复的评论ID
  "images": ["url1"]  // 可选，图片URL数组
}
```

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": 1  // 新创建的评论ID
}
```

**注意**:
- 需要验证用户登录状态
- 需要验证内容长度限制（最多255字）
- 创建评论后，需要更新 `interaction_post` 表的 `reply_count` 字段
- `images` 数组需要转换为字符串存储

---

### 7. 上传图片

**接口**: `POST /api/upload/image`

**请求头**:
- `token`: 用户登录token（可选，根据业务需求）
- `Content-Type`: `multipart/form-data`

**请求体**:
- `file` (File): 图片文件

**返回数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": "https://example.com/images/xxx.jpg"  // 图片URL
}
```

**注意**:
- 需要验证文件类型（仅允许图片）
- 需要验证文件大小（建议限制5MB以内）
- 需要将图片保存到服务器或OSS，返回可访问的URL

---

## 数据库字段说明

### interaction_post 表
- `images`: 存储图片URL，建议使用 JSON 数组格式 `["url1","url2"]`，或逗号分隔的字符串
- `author_id`: 需要 JOIN 用户表获取用户名

### interaction_reply 表
- `images`: 存储图片URL，格式同上
- `user_id`: 需要 JOIN 用户表获取用户名
- `target_user_id`: 如果存在，需要 JOIN 用户表获取目标用户名
- `answer_id`: 
  - `0` 表示直接回复帖子
  - 非 `0` 表示回复某个评论（该评论的ID）

---

## 注意事项

1. **用户认证**: 除了获取列表和详情，其他接口都需要验证用户登录状态（通过 token）
2. **数据关联**: 需要 JOIN 用户表获取用户名和头像信息
3. **点赞状态**: 需要维护用户点赞关联表，判断当前用户是否已点赞
4. **图片存储**: `images` 字段建议使用 JSON 数组格式存储
5. **评论树形结构**: 后端需要构建评论的树形结构，前端直接展示
6. **分页格式**: 使用 MyBatis-Plus 的 `Page<T>` 返回格式
