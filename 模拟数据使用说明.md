# 模拟数据使用说明

## 📋 当前状态

目前前端已配置为使用**模拟数据**，可以直接运行查看效果，无需后端接口。

## 🎯 查看效果

1. 启动前端项目：
```bash
npm run dev
# 或
pnpm dev
```

2. 访问社区互动页面：
   - 导航栏点击"社区互动"
   - 或直接访问 `/community`

## 📊 模拟数据内容

### 帖子数据
- 共 **10条帖子**，包含：
  - 不同数量的图片（0-6张）
  - 不同的浏览数、点赞数、评论数
  - 不同的发布时间
  - 部分帖子已点赞状态

### 评论数据
- 为部分帖子配置了评论数据
- 支持多级回复（树形结构）
- 包含回复目标用户信息

## 🔄 切换到真实API

当后端接口完成后，按以下步骤切换：

### 步骤1：修改 API 配置

打开 `src/apis/postApi.ts`，找到文件顶部的配置：

```typescript
// 是否使用模拟数据（后端接口完成后，将此值改为 false）
const USE_MOCK_DATA = true
```

将 `USE_MOCK_DATA` 改为 `false`：

```typescript
const USE_MOCK_DATA = false
```

### 步骤2：验证后端接口

确保后端接口已实现并正常运行，接口路径为：
- 基础URL: `/api`
- 接口列表见 `后端接口文档.md`

### 步骤3：测试功能

切换后测试以下功能：
- ✅ 帖子列表加载
- ✅ 分页功能
- ✅ 搜索功能
- ✅ 帖子详情
- ✅ 点赞功能
- ✅ 评论列表
- ✅ 发表评论
- ✅ 发帖功能
- ✅ 图片上传

## 📝 注意事项

1. **图片路径**：模拟数据中使用的图片路径是项目中的实际图片路径，如果图片不存在，会显示占位符
2. **点赞状态**：模拟数据中的点赞状态会在页面刷新后重置
3. **新增数据**：通过发帖功能新增的帖子会添加到模拟数据中，但刷新页面后会消失
4. **评论数据**：目前只为部分帖子配置了评论数据（帖子ID: 1, 2, 3, 4）

## 🗑️ 清理模拟数据（可选）

如果不再需要模拟数据，可以：

1. 删除 `src/mock/postMock.ts` 文件
2. 从 `src/apis/postApi.ts` 中移除模拟数据相关的导入和代码
3. 确保 `USE_MOCK_DATA = false`

---

**提示**：建议在后端接口完全实现并测试通过后，再删除模拟数据相关代码。
